{
  "name": "parking-system",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc",
    "pretest": "npm run test:db:setup",
    "start": "node dist/server.js",
    "test": "dotenv -e .env.test -- vitest run",
    "test:db:setup": "docker compose up -d --wait postgres && (docker compose exec -T postgres psql -U postgres -c \"CREATE DATABASE parking_test_db\" || true) && dotenv -e .env.test -- prisma migrate deploy",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "db:seed": "prisma db seed",
    "start:docker": "npx prisma migrate deploy && npx tsx prisma/seed.ts && npx tsx src/server.ts"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@types/node": "^26.1.2",
    "dotenv": "^17.4.2",
    "dotenv-cli": "^11.0.0",
    "prisma": "^7.9.1",
    "tsx": "^4.23.9",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  },
  "dependencies": {
    "@fastify/swagger": "^9.8.1",
    "@fastify/swagger-ui": "^6.1.1",
    "@fastify/type-provider-typebox": "^6.1.0",
    "@prisma/adapter-pg": "^7.9.1",
    "@prisma/client": "^7.9.1",
    "fastify": "^5.11.2",
    "pg": "^8.22.0",
    "typebox": "^1.3.11"
  }
}
